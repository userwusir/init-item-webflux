subprojects {
    dependencies {
        compileOnly project(':common')
        compileOnly 'org.springframework.boot:spring-boot-starter-validation'
        compileOnly "com.github.xiaoymin:knife4j-openapi3-spring-boot-starter:${knife4jVersion}"
    }
}